import Shared
import UIKit

final class DefaultIcons: Icons {
	let bicycle: UIImage = .init(resource: .iconBike)
	let bus: UIImage = .init(resource: .iconBus)
	let lpgCng: UIImage = .init(resource: .iconLpg)
	let motorcycle: UIImage = .init(resource: .iconMotocycle)
	let trailer: UIImage = .init(resource: .iconTrailer)
	let truck: UIImage = .init(resource: .iconTruck)
}
